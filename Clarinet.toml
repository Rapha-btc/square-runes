[project]
name = 'runes-square'
description = ''
authors = []
telemetry = false
cache_dir = './.cache'
requirements = []
[contracts.leb128-decoder]
path = 'contracts/leb128-decoder.clar'
clarity_version = 3
epoch = '3.2'

[contracts.multisig-verify]
path = 'contracts/multisig-verify.clar'
clarity_version = 3
epoch = 'latest'

[contracts.runes-decoder]
path = 'contracts/runes-decoder.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
