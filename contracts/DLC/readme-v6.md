**Thread:**

1/ OP_CAT would let Bitcoin verify ZK proofs natively - trustless bridges to L2s without oracles or multisigs. Today if you move BTC to another chain, someone has to sign. With OP_CAT, math signs instead.

2/ Satoshi disabled it over memory exhaustion concerns. Valid in 2010, but the 520-byte stack limit added since makes that attack impossible.

3/ "Bloat" concerns? Fees are low because blocks are underutilized. And no one's forced into anything - users still choose whether to sign an OP_CAT tx.

4/ The real debate isn't safety. It's whether Bitcoin stays a static rock or becomes programmable sound money. I think the latter wins.

---

Or as a single compressed tweet:

---

OP_CAT = trustless L2 bridges without oracles. Math signs instead of multisigs. Satoshi's memory concerns solved by 520-byte limit. Bloat fears make no sense when fees are this low. And users still choose to sign or not. Let Bitcoin evolve.

---

Which direction?
